apply plugin: 'java-gradle-plugin'

dependencies {
  compileOnly gradleApi()
  implementation project(':dex-tools')
}

gradlePlugin {
  plugins {
    blackObfuscatorPlugin {
      id = 'zym.top.blackobfuscator'
      implementationClass = 'top.niunaijun.blackobfuscator.gradle.BlackObfuscatorGradlePlugin'
    }
  }
}
